import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";


const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title:'SoftSell | Sell Unused Software Licenses',
  description: 'Get cash for unused software licenses. Highest payouts, secure transactions, fast payments.',
  keywords: ['sell software licenses', 'license resale', 'unused software', 'get paid for licenses'],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
      
          {children}
       
      </body>
    </html>
  );
}
